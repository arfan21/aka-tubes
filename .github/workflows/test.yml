name: test

on: [push]

jobs:
    build:
        name: Test Actions
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@master

            - name: Start Action
              run: |
                echo "TES ACTION"
